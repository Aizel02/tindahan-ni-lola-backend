version: "3.8"

services:
  db:
    image: mysql:8
    container_name: mysql-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: your_password
      MYSQL_DATABASE: sari_sari_db
    ports:
      - "3307:3306"  # ✅ change to 3307 to avoid local port conflict
    volumes:
      - mysql_data:/var/lib/mysql

  backend:
    build: .
    container_name: tindahanlola-backend
    restart: always
    depends_on:
      - db
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql-db:3306/sari_sari_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Manila
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: your_password
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_SHOW_SQL: "true"

volumes:
  mysql_data:
